import { CharProps } from '@/types/props';
import React, { FC } from 'react';

const LevelUp: FC<CharProps> = ({char, setChar}) => {
    // Your code here

    return (
        <div>
            {/* Your JSX here */}
        </div>
    );
};

export default LevelUp;